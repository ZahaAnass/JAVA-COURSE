# How to play audio with Java

You can play `.wav`, `.au`, and `.aiff` files using Java's `javax.sound.sampled`.

- Download free audio files from the YouTube Studio Audio Library.
- Put supported audio files into the `album` folder to test the code.
- Convert `.mp3` files to `.wav` with an external converter if needed.

Supported formats: `.wav`, `.au`, `.aiff`.